{
  "name": "lvce",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:browser:install": "playwright-core install chromium",
    "check": "node --check app.js && find functions lib scripts -name '*.js' -o -name '*.mjs' | xargs -n1 node --check",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "playwright-core": "^1.62.1",
    "wrangler": "^4.30.0"
  }
}
